
class Player 
{  
  constructor(userId, isSick, isDead, currentHand) 
  {
    this.userId = userId;
    this.isSick = isSick;
    this.isDead = isDead;
    this.currentHand = currentHand;
  }
}

export default Player;
